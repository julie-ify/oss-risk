OSS-RISK

### Server

create a fresh venv `python -m venv venv`

install dependencies `pip install -r requirements.txt`

run the server using `flask run --port 3000`

### Client

install dependencies `npm install`

run the client using `npm run dev`
